# ==========================================================================
# L1 loss (Mean Absolute Error).
# Стандарт для задач super-resolution.
# Более устойчив к выбросам, чем MSE, даёт меньше размытия.
# ==========================================================================

# Класс PyTorch — инстанцируется через hydra.utils.instantiate().
_target_: torch.nn.L1Loss
