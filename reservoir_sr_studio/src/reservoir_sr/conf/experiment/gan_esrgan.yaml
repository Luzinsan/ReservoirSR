# @package _global_
#
# Experiment: ESRGAN-like training with RRDB generator.

defaults:
  - override /task: gan
  - override /model: gan_esrgan
  - override /trainer: default
  - override /data/condition: none

globals:
  experiment_name: gan_esrgan

trainer:
  max_epochs: 150
  gradient_clip_val: 1.0
