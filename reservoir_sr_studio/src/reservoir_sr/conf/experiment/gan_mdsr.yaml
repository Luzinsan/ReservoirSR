# @package _global_
defaults:
  - override /model: gan_mdsr
  - override /trainer: default
  - override /data/condition: none

task: gan

globals:
  experiment_name: gan_mdsr

trainer:
  max_epochs: 80
  gradient_clip_val: null
  check_val_every_n_epoch: 2