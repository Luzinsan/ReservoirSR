# ==========================================================================
# Condition-вектор полностью отключён.
# Ablation: baseline без скалярной информации — чистый image SR.
# Размерность: 0 (condition-тензор не создаётся).
#
# ВАЖНО: при использовании этого конфига модель должна иметь
# condition_dim=0 (см. experiment/ablation_no_cond.yaml).
# ==========================================================================

# Все группы отключены.
dynamic: false
static: false
layers: false
